# cleanup: remove frontend and unrelated files, keep firmware and CI

Remove TypeScript frontend, build artifacts and non-firmware files to produce a firmware-only repository layout. Keep flash and terminal helper scripts and GitHub Actions for auto compilation.
